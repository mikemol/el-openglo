<!-- DERIVED. Regenerated every tick by scripts/paths_forward.py --render.
     Edits here vanish; edit .claude/paths-forward.json (or tell the loop). -->
# paths-forward — el-openglo

heartbeat 2026-09-21T16:52:31+00:00 · job `b5a33efd` · counter 39 · hash `eae31090a77681b4`

| # | status | title | blocked on | next bounded step |
|---|---|---|---|---|
| W1 | done | Restore the borrowed hooks' selftests: scripts/ needs __init__.py and hook_cmdparse (symlink into substrate) — @HOOKS | — | — |
| W11 | done | Answer summit's three letters in inbox/ (axis 4 Δ-sandbox root, axis 5 wrapper shape, axis 10 vendoring posture) — answers go in the axis files under the operator's name | — | — |
| W2 | done | Structural-query hook reports an EMPTY routing table — @ROUTES | — | — |
| W3 | done | The ghost that renders is not the ghost that is gated: alpha 0.45 lives between check and screen, 6 of 6 variants — @GHOSTCOMP | — | — |
| W4 | done | cotype warrants.bib's `enables` field is DROPPED by paperkit until catalog/cotype/paper.toml declares consumer_fields | — | — |
| W5 | done | SegmentChar as the one geometry substrate under wallpaper/clock/marquee/plymouth — cotype ⊕SEGMENT-SUBSTRATE | — | — |
| W6 | done | Arbitrary text reaches the matrix by rasterising a font into it — cotype ⊕MATRIX-FONT-INPUT | — |  |
| W7 | done | Segment projection chain: real fontTools ingest → calibrate bandwidth/tau → validate against the authored 44 → descenders in 22-seg — cotype ⊕SEG-FONT-PROJECT, ⊕SEG-PROJECT-CALIBRATE, ⊕SEG-TABLE-VALIDATE, ⊕SEG22-DESCENDERS | — |  |
| W8 | done | ⊕SEGMENT-ROLLOUT: every surface draws the palette fg/fg_in at ghost_alpha — 24 of 24 emissions differ; SegmentChar.qml has zero consumers (+ ⊕GHOST-DENSITY) | — | — |
| W9 | done | ⊕PLYMOUTH-VECTOR: the boot splash pre-rendered at the target resolution (or SVG) — ⊕CLOCK-VECTOR done | — | — |
| W10 | done | TUNE bucket: cursor inherit, icons inherit, solver-owned UI tokens, Alt+Tab switcher — cotype ⊕CURSOR-INHERIT, ⊕ICONS-INHERIT, ⊕SOLVER-UI-TOKENS, ⊕TASKSWITCH | — | — |
| W12 | blocked | Gentoo overlay under overlay/ + a live ebuild (git-r3, this repo) that runs the emitters and installs what make_deb installs, + check_ebuild.py witness | mikemol | ba7e4c3: overlay carries dev-python/colorspacious (from ::guru, thin manifest); check_ebuild deps_resolve arm + --deps (5 of 5 atoms). Pushed through d14f963; ba7e4c3 UNPUSHED. OPERATOR (no restart): after emerge of d14f963+ and plasmashell --replace — widget settings page draws? clock halo/weight look right? Applying EL-Azure ...
The requested theme "EL-Azure" is already set as the theme for the current Plasma session.
The requested theme "EL-Azure" is already set as the theme for the current Plasma session.
Successfully set the wallpaper for all desktops to the image /usr/share/wallpapers/EL-Azure/contents/images/1920x1080.png
  Wallpaper: set to EL-Azure
  Konsole: default profile -> EL-Openglo-EL-Azure (new tabs)

  Clock: EL segment clock added to panel (if not already present)
  Chrome: load /usr/share/el-openglo/chrome/EL-Azure
          via chrome://extensions -> Developer mode -> Load unpacked
  Konsole: pick 'EL-Azure' in Settings -> Edit Profile -> Appearance
  Lock screen: watch-face wallpaper set (auth unchanged)
Done. Some changes (GTK, Kvantum) may need apps to restart.
Login screen (SDDM): sudo el-openglo-sddm EL-Azure  (sets the phosphor login background)
Boot splash (Plymouth): sudo el-openglo-plymouth EL-Azure  (sets the phosphor boot splash)
Living watch face:      el-openglo-live EL-Azure  (animated clock on desktop + lock)
Notification ticker:    el-openglo-notify EL-Azure  (marquee subsumes popups) → new Konsole tab recoloured? re-Apply Global Theme → kdeglobals ColorScheme flips? MACHINE remaining: gate_ledger path mismatch (substrate symlink — fix THERE, run both selftests). Nudge ONCE for the probes at tick 6, then leave it. |
| W13 | blocked | Upstream contribution routes for every theme target — where each is published, how to submit, licensing gates, whether generated themes are welcome | m, i, k, e, m, o, l, (, h, u, m, a, n, ) | MACHINE half done at 1c61346: catalog/publishing.md (15 emitters → venues; OCS ids from the letter; wallpaper id 299 read from the cached listing) + check_publishing.py @PUBLISHING (52). HUMAN half: open store.kde.org/product/add once and read the form — licence selector? preview requirements? does Aurorae go to 114 or 717? — then record as [WEB] rows. Nudge ONCE, then leave. |
| W14 | done | Union theme emitter — the palette emitted as a KDE Union style description (CSS input via cxx-rust-cssparser; Plasma-SVG input defunct 2025-10-09), the sink that subsumes make_kvantum + make_plasma + the QQC2 half of breeze | — | — |
| W15 | done | Firefox theme emitter — the palette as a WebExtension theme (manifest.json "theme" key), the nearest sibling of make_chrome | — | — |
| W16 | done | Windows theme emitters — a .theme file (desktop colours, accent, wallpaper) and a Windows Terminal colour scheme (JSON), from the same palette | — | — |
| W17 | done | GNOME theming — what of the palette can reach GTK3, GTK4/libadwaita and GNOME Shell, and through which mechanism (gtk.css, adw-gtk3-style theme, Shell user-theme, or the libadwaita accent API), given ⊕GTK-ADW says pure-libadwaita apps honour no override | — | — |
| W18 | done | Android — how a solved palette reaches an Android device: Material You derives its OWN tonal palette from the wallpaper (Monet), so the honest emit may be a wallpaper whose seed colour steers Monet toward the palette, plus a check that Monet's derived scheme agrees with ours | — | — |
| W19 | done | Web / IndieWeb theme — the palette as CSS custom properties (one :root block per variant, prefers-color-scheme mapping Off/Lit), the sink every site theme reads; the operator's future IndieWeb site is the first consumer | — | — |
| W20 | done | The pre-commit hook warms the palette cache before the worklist gate — a cold .palette-cache.json makes ~13 checks time out at once and the hook refuses a green tree | — | — |
| W21 | done | Restore ⊕STROKE-WEIGHT and ⊕BLOOM to the clock — both CLOSED in COTYPE.md (sessions 40-41, deb 1.10/1.11) and lost in the recovery; templates/clock-main.qml draws lit and ghost at the same segThick with no MultiEffect (only the orphan Effects import survived). Operator, live 2026-09-21: ghost too close to lit; lit should read BIGGER | — | — |
| W22 | done | Digit PITCH is substrate, derived from a published module spec — segment_topology fixes the cell (2L×4L), naming and masks, but the ADVANCE per digit and per colon, the slant, and the stroke:height ratio are authored per surface: make_wallpaper 1.55L/0.72L, live wallpaper 2.6U/0.8U, clock digitGap 0.45 (placeholder, 2026-09-21). Operator: 'aren't there published standards for this?' — yes: multi-digit segment-module datasheets (digit pitch, character width, segment width, slant, colon offset) and segment fonts (DSEG advance widths) | — | — |
| W23 | done | The palette's solved ghost EQUALS lit on EL-Azure and EL-Amber — check_ghost_surfaces --map (2026-09-21): fg_in == fg for both Off variants ((153,204,255); (255,212,153)); EL-Openglo differs slightly ((141,235,217) vs (153,255,235)). On those variants the ghost/lit distinction is alpha-only, no colour channel — the operator's live 'ghost too close to lit' on Azure at the PALETTE level; every surface is faithful to a degenerate solve and every check passes | — | — |
| W24 | done | Rebuild the font family — ⊕SEG-FONT, ⊕SEG-FONT-TTF, ⊕DOT-FONT, ⊕DOT-FONT-TTF, ⊕DOT-FONT-DESC are CLOSED in COTYPE.md and KNOWN-LOST (check_symbol.LOST, sweep 2026-09-21): make_font.py is a recorded partial nobody imports, fonts/ is absent, make_deb maps fonts/ only if present | — | — |
| W25 | done | Rebuild the three small KNOWN-LOST closures: ⊕VER-WIDGET-ICON (make_preview.icon_svg — the clock plasmoid's icon from the scheme; make_deb SKIPs it per variant), ⊕QML-SANITY (qml_sanity.py — the staged QML is unlinted; qmllint at /usr/lib64/qt6/bin exists here), ⊕RENDER-GATE (render_qml.py exists but make_deb does not run it as the fourth gate the closure at :4545 states) | — | — |
| W26 | done | Re-author the 5x8 descender matrix table (⊕DOT-FONT-DESC, the last KNOWN-LOST closure): display_types.FONT5x8 with true descenders for g j p q y (and the lowercase set the closure at :1496 lists), MatrixDisplay(5, 8) in DISPLAYS, the marquee/matrix surfaces reading it, EL-Matrix-5x8.ttf from matrix_contours(cols=5, rows=8) | — | — |
| W27 | done | ⊕SEG-DOTPRODUCT-TEMPLATES — curvature-aware segment templates in glyph_match: the measured ceiling per glyph class (s75: round walls 0 O D B miss a/d and gain g; diagonals-to-centre A V 7 W; the '1' convention) | — |  |
| W28 | done | ⊕SEG-FONT-PROJECT + ⊕SEG22-DESCENDERS — the projection pipeline validated across ALL 44 (the 36 plus the 8 symbols the log counts) and the 22-seg table gaining lowercase descender glyphs | — |  |
| W29 | done | ⊕GHOST-DENSITY — does the marquee's dot field at the solved ghost_alpha read as the same texture the segment ghost does? Measure, not assume: the seen ghost of a dot cell (dotFill 0.82 of the pitch) vs a stroke at the module stroke ratio | — |  |
| W30 | ready | Font COMPILER (operator, 2026-09-21): arbitrary TTF -> precompiled (segment set @22 per glyph, matrix column bytes per glyph) tables, so a font (Liberation, Unifont, ...) is derived ONCE and the emitters read the derived table — the two halves already exist (display_types.font_extension = the matrix half; glyph_match.match + ST.project = the segment half); the compiler is the one entry point + an on-disk table format + the emitters reading it. Operator: 'later; not an immediate priority unless it unblocks something' | — | Only when it unblocks something. When taken: state the table format (JSON: font path+hash, charset, per-glyph {seg22, matrix5x8, class, jaccard}), one compile_font(path, charset) in a new font_compile.py that calls the two existing halves (no re-derivation), and make display_types.font_extension read a compiled table when given one. Bitmap fonts (Unifont) route through project_font.raster_ink — measure one glyph first. |
| W31 | done | TUNE trio: ⊕ICONS-INHERIT + ⊕CURSOR-INHERIT (an icon theme and a cursor theme that INHERIT Breeze — index.theme with Inherits=, named EL-<variant>, wired into make_deb and the LnF defaults so Global Theme sets them) then ⊕TASKSWITCH (an Alt+Tab tabbox/window switcher in the Plasma style). ⊕SOLVER-PERF and ⊕PANEL-LAYOUT already read done by their witnesses; the bucket goes green when these three are present | — |  |
| W32 | done | RESIDUE sweep, the cheap two: ⊕NOTIFY-SEGRENDER (its criterion at :3568 is 'the ticker renders in the actual 7-seg/DOT primitive' — the dot matrix ticker of s77 satisfies the DOT half; propose the COLLAPSE into ⊕NOTIFY-MATRIXRENDER: re-aim its witness at MatrixChar dots, COTYPE closure naming the collapse, CLOSED) and ⊕SUPERSAMPLE-WP (:2583 'the wallpaper generator supersamples at generation time' — measure how make_wallpaper rasterises its PNG (cairosvg at 1920x1080 from the SVG?); if the SVG is rasterised at 1x, render at 2x and downscale with a box filter in the PNG step, check with a synthetic edge (aliasing energy falls), close) | — |  |
| W33 | ready | ⊕SEGMENTCHAR-ADOPT (:4419) — a shipped surface instantiates templates/SegmentChar.qml rather than drawing segments in its own idiom. The most load-bearing residue: SegmentChar is the one place bloom/weight/pitch could reach every segment surface at once (clock, live wallpaper, splash), instead of each carrying its own. Measure first: what SegmentChar.qml exposes today vs what clock-main.qml draws (strokeLit/strokeGhost, bloom MultiEffect, digitGap/pitch); the gap is the port | — | Port step (1)+(2) into templates/SegmentChar.qml ONLY (no surface adopts yet): properties weight (default 1.0), ghostWeight (0.81), bloom (1.5), strokeBase derived from the module (metrics: stroke/1.25 of the cell like make_clock._metrics_holes — pass as a hole or derive from u); litHalf = u*strokeBase*(1+0.25*weight)/2, ghostHalf = u*strokeBase*ghostWeight/2; REPLACE the wider-copy underlay with a lit-only layer under MultiEffect blur (blurMax = litHalf*4*bloom, opacity 0.75, layer.enabled: bloom > 0); rounded ends optional. Then: check_symbol's _bloom_is_blur/_stroke_ratio gain a SegmentChar read (both templates must satisfy), rm + capture_baseline make_segment_display segment_char_component SegmentChar.qml, qml_sanity, and a headless render of the component alone if render_qml has (or gains) a harness for it. Commit; the clock's adoption is the step after. |
| W34 | done | ⊕VER-MARQUEE (operator, live 2026-09-22, screenshot): (a) the ghost pips SCROLL WITH the glyphs — a real board's unlit LEDs are fixed; the dot field must be a static bezel-to-bezel matrix and the lit glyphs scroll OVER it (lit dots light the field's own positions — snap the text to the field's cell pitch, or overlay); (b) the pips only span the message width, not the widget; (c) nothing configurable — the widget has no config page (the clock has one: templates/clock-config.qml/.kcfg + cfg_ aliases; import org.kde.kcmutils as KCM): speed, dot pitch/fill, ghost weight, blink/idle face, seconds of dwell. 'Clock looks amazing.' | — |  |
| W35 | ready | ONE THEME, VARIANT SELECTABLE (operator 2026-09-22): collapse the six-of-everything into one set of packages that read the ACTIVE EL colour scheme at runtime — the clock, marquee, live wallpaper, switcher and LnF become one each; variant selection = applying a colour scheme (the .colors files stay six, that is how Plasma variants work). Design first: which solved tokens are reachable live from QML (Kirigami.Theme standard roles: highlight/text/background… — the palette solves fg/fg_in/view/hover/sel into those roles) and which are NOT (the [EL] group: ghost_alpha, ghost_alpha_glanced — a KConfig read the QML cannot do directly; options: bake ghost alpha into a standard-role relation, or ship a tiny helper, or read via a Plasma DataSource); what the gates become when colours are no longer in the emitted bytes (check_ghost_surfaces reads holes today; parity baselines lose the per-variant pairs) | — | Measure: a table of every colour hole each surface template fills (lit/ghost/ground/hot/ghostAlpha… via check_ghost_surfaces --map and the  in templates/) against the standard KColorScheme roles the palette writes them into in make_schemes (which token lands in which [Colors:*] role) — the MAPPING decides whether a surface can go live. Then a proof of concept on the switcher (smallest surface): Kirigami.Theme.* in place of the four holes, ONE package, and does it follow plasma-apply-colorscheme EL-Azure -> EL-Amber without reinstall (render_qml cannot see the live scheme; this is a ⊕VER probe). Record in COTYPE as a new symbol with a witness. |
| W36 | ready | PHOSPHOR CURSORS (operator 2026-09-22: 'isn't the cursor supposed to inherit something related to the color scheme?'): cursors cannot follow the colour scheme by inheritance (fixed images), but the host has /usr/share/icons/breeze_cursors/cursors_scalable/ (SVG sources) — an EL cursor set is a DERIVATION: recolour the SVGs from the palette (lit for the arrow fill? ground for the outline?) and build with xcursorgen per size. Cheap to measure first: how many SVGs, which colours they use (a census), whether xcursorgen is installed (x11-apps/xcursorgen) | — | Census only: count the SVGs in cursors_scalable, the distinct fill/stroke colours across them (an SVG colour census tool — check_chrome/check_windows style, or a small mode on check_inherit), and whether xcursorgen exists here; record; decide fill/outline mapping from the palette relation (the arrow must read on BOTH grounds it crosses — a cursor is a glanced-at surface over unknown ground, so it needs an outline, i.e. two tokens). |
| W37 | ready | PLASMA 6.7 LEGACY THEMES — named (announcement, 2026-06): OXYGEN (KDE 4's default: kstyle + kdecoration + Plasma Style + icons/cursors/sounds) and AIR (the light Plasma Style), 'brought up to par with Breeze' with light/dark/twilight Global Themes, plus the Air and Horos wallpapers; UNION ships as a tech preview (W14 already emits six Union styles — installed, not active here). Theming 'over' Oxygen = a second LnF flavour per variant naming Oxygen's engines (widgetStyle=Oxygen, kwin library org.kde.oxygen) — both read KColorScheme, so the EL scheme already colours them — plus a MEASUREMENT of the Oxygen/Air Plasma Styles: do they follow the system scheme (a colors file / FollowsColorScheme in their metadata) or bake their own? | — | Measure on the host: is kde-plasma/oxygen installed (ls /usr/share/plasma/desktoptheme/ for oxygen + air; the Oxygen kstyle plugin; /usr/share/plasma/look-and-feel/ for org.kde.oxygen*); Read oxygen's desktoptheme metadata.json and whether a colors file exists beside it (a Plasma Style without one follows the system scheme; with one, its colours are fixed); Read a stock org.kde.oxygen LnF defaults to see exactly which engines it names. Then decide: an engine=oxygen flavour of build_lnf_packages (six more LnFs — or, if W35 lands, one LnF with an engine choice) and whether make_plasma should emit an Oxygen-shaped Plasma Style (only if Oxygen's bakes colours). |
| W38 | done | MARQUEE RING BUFFER (operator live 2026-09-22: with watch notify-send hi the scrolling text blips in and out with the popups). Cause, from templates/marquee-main.qml: rebuild() replaces root.tickerText on every notifModel onCountChanged, so the Repeater tears the Row down mid-scroll. Design (operator): double-buffered rings — the SCROLLING ring loops as long as its notifications are active; a PENDING ring collects arrivals; the swap happens at a full rotation (the NumberAnimation's loop boundary), never mid-scroll; a notification that left the model is dropped at the boundary too | — |  |
| W39 | ready | HTML NOTIFICATION BODIES (operator 2026-09-22): some senders emit HTML bodies; parse the markup and let styling drive the dots — bold/strong as lit weight or glow, font color / span color as a HUE on the lit token where the palette allows (a relation gated by the same floors, never a raw literal on the phosphor), i/em perhaps as a slant or ghost weight. CENSUS FIRST: what markup actually arrives (Plasma's notification spec allows b i u a img; senders also emit font color and span style) | — | The widget half: (1) make_notify_marquee.main_qml emits a hueTable hole (make_palette.hue_table(lit, ground, ghost) as a JS array of 12 hex colours; fallbacks already equal fg) — the registry stays the display's, the table is the variant's; (2) MatrixChar gains per-character overrides: litColorOverride (color, default transparent = use litColor) and glowBoost (real, default 0); (3) the marquee's Repeater reads root.tickerRuns at each index (a small runAt(i) function) — bold -> glow 1.25 (the clock's weight ratio), colour -> the nearest hue bucket from the table (hue of the parsed colour name/hex: a JS hex->hue is fine, names via a small map or ignore); (4) a harness case: a body with a font colour yields a run with color set; (5) check_ghost_surfaces still reads the emitted litColor/ghostColor holes (it must — the override is per char). qml_sanity; parity re-capture. Then W39 done; COTYPE s94 (⊕VER-MARQUEE). |

## evidence

- **W1** — scripts/__init__.py + scripts/hook_cmdparse.py -> ../../substrate/scripts/hook_cmdparse.py; struct-tools SKILL.md gained two filename-claim rows (54/55 → 55/55). `python3 scripts/check_hooks.py` → "2 of 2 hook selftests pass from this repo".
- **W11** — Operator declined all three (AskUserQuestion, 2026-09-20). Decline sent to summit-b0 via SendMessage msg_id=e43c0981 (queued; delivery notice pending). Recording in the axis files is summit's convention, left to summit.
- **W2** — Same cause as W1, as predicted: `python3 scripts/check_routes.py` → "the local routing table has 20 row(s)" (now 22), exit 0.
- **W3** — DONE (tick 8): floor in APCA — cvd_gate.GHOST_VISIBLE_LC=25 (derived from Off variants composited 29.8/25.4/29.9, rounded down) + feasible_ghost_floor_lc; WCAG floor kept as residue; ghost_solve.solve_floor_t/alpha_min in Lc; alpha rounded UP (0.503); check_ghost_composite floor/refusal/--compare in Lc, selftest 16/16 incl. the Lit case; relations.md §3b records the relation, the alpha derivation and the one-metric argument; baseline re-captured. `python3 scripts/check_ghost_composite.py` → 6 of 6 clear, worst Lc 25.0. `python3 scripts/worklist_gate.py` → paperkit-gate: PASS, 44 claims resolve. Note: a COLD .palette-cache.json makes ~13 checks time out under paperkit; warm it (any palette check) before reading the gate.
- **W4** — catalog/cotype/paper.toml: consumer_fields = ["enables"]. `python3 scripts/worklist_gate.py` no longer prints the DROPPED warning; COTYPE-WORKLIST.md ≡ projection (unchanged).
- **W5** — DONE (tick 12, commit e212032): glyph16(strict=)/has_glyph contract; digit paths strict; --coverable 3 of 3; @SUBSTRATE-COVERABLE green; COTYPE.md session 67 + closure section + ledger; `check_symbol.py SEGMENT-SUBSTRATE` → present; BUILD bucket 1 of 2 open (⊕MATRIX-FONT-INPUT only). Reading apparatus fixed twice: check_symbol witness now runs the tool (was a noun regex); cotype_index selftest rule 4 on a synthetic doc (was pinned to this symbol).
- **W6** — b1e4b7f: display_types.font_extension + registry font5x8; make_notify_marquee.matrix_font (decision recorded; BDEPEND liberation-fonts); MatrixChar '?' fallback; sample re-rendered; parity pair pinned to PARITY_FONT with SKIP; ⊕MATRIX-FONT-INPUT + ⊕NOTIFY-MATRIXRENDER in CLOSED (35 witnessed); cotype BUILD bucket closed out; COTYPE s77
- **W7** — 5f2fc5a: glyph_match.calibrate_projection + SW_BAND=0.85 solved; check_projection --calibrate/--frame; mean Jaccard 0.58->0.65, 9/36 exact; frame hypothesis rejected (fit 0.43), crossbar stroke-width defect fixed; COTYPE s75; ⊕SEG-PROJECT-CALIBRATE in check_symbol.CLOSED (33 witnessed). Residue -> ⊕SEG-DOTPRODUCT-TEMPLATES (witnessed open, RESEARCH)
- **W8** — DONE (tick 16, commit 2dca20e): clock reads fg/fg_in/ghost_alpha, segments+colon at solved alpha; make_schemes emits [EL] GhostAlpha into every .colors; parse_scheme reads ghost+ghost_alpha; plymouth reads both (ghost_from residue). check_ghost_surfaces 24 of 24; @GHOST-SURFACES green; ⊕SEGMENT-ROLLOUT closed (COTYPE session 70, Four gates line); ⊕SEGMENTCHAR-ADOPT opened as residue with a witness. check_palette_chain selftest fixture (pinned to C.derive_ghost) moved to a planted module. Worklist 44/44. ⊕GHOST-DENSITY remains the RESEARCH follow-on.
- **W9** — 7a0e543: mechanism measured (Image.Scale in script-lib-image.c; target res only at boot) → ASSET_U=192 oversampled, script scales to 0.20·sh, module pitch layout; COTYPE session 72 closure; witness reads the emitted script; TIER 3 bucket closed out (@TIER3 green). ⊕VER: plymouth not installed on luthen — the boot itself waits.
- **W10** — b176de5: states measured at q 0.06-0.75 (authored nudges never separated them); relations §4b + solve_state_steps (smallest feasible step, infeasibility named); check_states @STATES (56); the KNOWN_INFEASIBLE pin fired (2.94→3.33) and left the table; COTYPE s73 closes ⊕SOLVER-UI-TOKENS (31 witnessed). OPEN by name: sel_in, link/visited on selection, sel_alt/sel_act, panel ladder; ⊕SOLVER-PERF untouched. ⊕VER: the Off variants' hover ring is darker now.
- **W12** — EMERGE #3 INSTALLED; ⊕VER #2 previews on screen. ⊕VER #3 (Apply EL-Azure, "Appearance settings" only): PANEL recoloured (plasmarc Theme name=EL-Azure — measured), Qt widgets NOT (kdeglobals ColorScheme still BreezeLight — measured), wallpaper NOT (expected: Image= is under "Desktop and window layout", unticked). Our defaults has the same [kdeglobals][General] ColorScheme= shape as org.kde.breezedark.desktop; plasma-apply-colorscheme --list-schemes lists all six EL schemes. Open question: did the KCM skip Colors (its "Choose what to apply…" sub-selection) or fail to apply it. Packaging half CLOSED; guru colorspacious + host hardlink dedup still open.
- **W13** — 1c61346; inbox/2026-09-20-linux-sources-contribution-routes-per-target.md; catalog/ocs-categories.xml
- **W14** — 8a7c449: make_union.py (one style per variant; Breeze imported; indicator/focus/hover alphas solved by ghost_solve.alpha_min over the scheme's pairs — EL-Openglo indicator 0.396 vs Breeze's authored 0.4; OPEN list kept authored), check_union.py @UNION (worklist 48; 18 of 18 arms), staged under usr/share/union/css/styles/, apply writes UNION_STYLE_NAME env hook, relations.md §3d. ⊕VER after emerge + a LOGIN (HOLD ~2026-09-24): hover fills, focus ring, indicators follow the solve; engine cascade of a later :root is the unproven half.
- **W15** — cc3afe0: make_firefox.py (38 keys → roles; borders/separators = seen ghost; hover/active fills = highlight over toolbar at glanced/looked floors), check_firefox @FIREFOX 36 of 36 (web-ext lint SKIP), worklist 51, staged usr/share/el-openglo/firefox/<v>/. Route for W13: AMO unlisted signing. ⊕VER: load in Firefox via about:debugging (temporary) or sign on AMO.
- **W16** — a3f9937: make_windows.py (.theme: required sections, colour table, ColorizationColor accent, DesktopBackground wallpaper) + check_windows @WINDOWS 30 of 30; make_konsole.ansi_table + alacritty/foot restored + windows-terminal/termux + check_terminals @TERMINALS 30 of 30 (Konsole scheme byte-identical, cmp). Worklist 50. Operator ⊕VER needs a Windows box: copy usr/share/el-openglo/windows/<v>/ and open the .theme.
- **W17** — ef8c504: ⊕GTK found with NO build (third closed symbol this week) — make_gtk.py rebuilt (ADW_VARS 40 → roles; ADW_NAMED from the libadwaita CSS-variables reference read 2026-09-21; GTK3_DEFINES for adw-gtk3), check_gtk @GTK 24 of 24 (name typo red; selection pair at the palette's floor), CLOSED witness in check_symbol (3 of 3), staged where el-openglo-apply already copies from. Reach measured: libadwaita reads gtk.css :root variables in every app — the sanctioned channel. NOT reached: GNOME Shell (user-theme extension) — residue. ⊕VER needs a GTK app after emerge + el-openglo-apply.
- **W18** — 5b25d5e: check_monet.py @MONET (55) — TonalSpot/0 seeded from lit: hue kept <1° on 6/6, AA pairs 6/6, dark surface 2.5-3.4 dE from ground on the Off variants; Lit grounds unreachable (Monet light surface near-white) — recorded in catalog/android.md. Emit = wallpaper + documented seed (the lit colour); Termux already ships. Not built with reasons: themed icons, launcher themes.
- **W19** — DONE (tick 22, commit 93fc427): make_css.py → catalog/el-openglo.css (6 variants; --el-<key> per token; both alphas; :root = EL-Openglo, prefers-color-scheme:light = -Lit; seen ghost as color-mix per mode); check_css.py wired as @CSS, 228 of 228 properties equal the palette; selftest sees stale/dropped/foreign/non-mix/mis-mapped; tinycss2 declared (tooling extra); make_css in the emitter roster; worklist 46 of 46. IndieWeb specifics wait for the site.
- **W20** — DONE (tick 23, commit 596da31): make_schemes.py --warm (a mode); hook step 4a `run "palette-cache"` before the gate; step 4 captures the gate output and prints it on refusal. CORRECTION from the gate ledger: the three refusals took ~5 s (fast failures), NOT cold-cache timeouts — the reason was discarded by >/dev/null 2>&1; the cascade I saw was from running the gate by hand after a solver edit. Both fixed. The hook exercised itself on this commit.
- **W21** — 3e498c1 (rebuild + @REGRESSIONS); dc55cb7: ⊕VER found config page blank (org.kde.kcm is KF5 → org.kde.kcmutils, blank since recovery) and the halo a haze (64px blurMax + brightness on a 3px stroke) — blur now scaled by stroke×slider; d14f963: scripts/render_qml.py — headless render via Qt's qml runner + grabToImage, RHI forced (offscreen defaults to the SOFTWARE scene graph where MultiEffect draws nothing), --pixels census; rendered clock at panel height and wallpaper look right. UNPUSHED: c8f9995 3e498c1 dc55cb7 d14f963.
- **W22** — 9e8e27a: four datasheets (Kingbright SA56-11/DA56-11/CC56-12, Avago HDSP-B0xG) agree — pitch 12.7 for H 14.22, single package = char 8.0 + 2×2.35, colon adds no advance. segment_topology.MODULE_METRICS + metrics(H); all three surfaces read it; check_geometry_source --coverable 4th arm (perturbed pitch reaches 3 of 3); substrate selftest asserts the witnesses agree; STANDARDS.md row. Rendered: clock + wallpaper 23:46 at module pitch. RESIDUE: relations.md §geometry entry not written (pitch is a floor relation in prose here, not in the relations table); marquee sample re-renders differently with no input change; slant 8° not applied anywhere yet.
- **W23** — 0511aa9: the defect was the global alpha = max(alpha_min) — reaches every FLOOR, not every CEILING; Azure's seen ghost sat at Lc 25.0 vs 29.8 elsewhere; declared==lit was the side effect. Now max(max(alpha_min, 1 - t_ceiling)) → 0.566; every variant within 1 Lc of the ceiling; check_ghost_composite holds looked-at ghosts to the TARGET (TARGET_SLACK 1 Lc), selftest plants Azure's shape. .colors/css/clock/samples/baselines regenerated; relations §3b. ⊕VER on Azure after emerge: ghost slightly MORE visible than before (ceiling), consistent across variants; fainter = a ceiling change.
- **W24** — 1ab38ca: glyph_contours/matrix_contours → one build_ttf; EL-Segment-{7,16}.ttf + EL-Matrix-5x7.ttf + SVG fonts under fonts/; source-relative orientation gate; check_font @FONT-EMIT (54); make_font in STAGE, mapping unguarded; fonttools to base deps + ebuild; 4 symbols LOST→CLOSED (28/1). Remaining: ⊕DOT-FONT-DESC — the 5x8 table is gone (W26).
- **W25** — e570137: icon_svg ('12' over ghost at module pitch), qml_sanity.py on qmllint keyed by diagnostic id (Plasma context props not errors), render gate in make_deb (SKIP under SANDBOX_ON — the offscreen platform opens /dev/nvidiactl, a sandbox violation); BDEPEND +qtdeclarative; check_deps fixture de-pinned; three symbols LOST→CLOSED (24 witnessed / 5 lost).
- **W26** — dc4b748: FONT5x8 authored via _cols row strings; baseline as a LINE (make_font baseline=); EL-Matrix-5x8.ttf with negative descent; check_font descender arm + off-by-one fixture; 'g' rendered with its tail below the baseline; check_symbol LOST now EMPTY (29 witnessed / 0 lost / 23 artifact-free).
- **W27** — 877cb1b: glyph_match._arc_field + SAGITTA=-0.15 solved (--calibrate --arcs); per class round 0.60->0.62, straight 0.77->0.79, diagonal 0.67->0.68; overall 0.653->0.672, 10/36 exact; fields vectorised (_band) after --regressions hit 3m15; ⊕SEG-DOTPRODUCT-TEMPLATES in CLOSED (36 witnessed); COTYPE s79
- **W28** — d8823f8: segment_topology.LETTERS22/DESCENDER_GLYPHS/glyph22 + substrate arms (caught g==q); ink_field frame=lowercase (+font_xheight); validate at 22: 0.37, every tail bar hit; registry 22 carries lowercase; ⊕SEG22-DESCENDERS in CLOSED (39 witnessed; --regressions 58s CPU); RESEARCH bucket green; COTYPE s84
- **W29** — 3a59723: format_coverage + measure_formats; coverage 7 0.396 / 14,16 0.801 / 22 0.837 / dots 0.528; ⊕GHOST-DENSITY in CLOSED (37 witnessed; --regressions 55s); COTYPE s81
- **W30** — 
- **W31** — a5f8a75: templates/taskswitch-main.qml + make_taskswitch.py + make_deb wiring + LnF [kwinrc][TabBox]; scripts/check_taskswitch.py @TASKSWITCH (worklist 59); check_ghost_surfaces enumerates the switcher (30/30); ⊕TASKSWITCH in CLOSED (42 witnessed; --regressions 61 s CPU); TUNE bucket green; COTYPE s86
- **W32** — 751e394 (+ its parent): ⊕NOTIFY-SEGRENDER closed by collapse into MATRIXRENDER; ⊕SUPERSAMPLE-WP closed on measurement (2560x1440 + glow filter); coherence caught SOLVER-PERF/PANEL-LAYOUT dropped without closures — closed properly (s87b); 46 witnessed; RESIDUE now 6: HDR-EMIT KNB2 KVT2 PLA2 SEGMENTCHAR-ADOPT VER-SYSCLOCK
- **W33** — 3755cc7: COTYPE s88 property table (geometry, cell, lit/ghost stroke, end gap, alpha, bloom, pitch/colon, slant) + what SegmentChar must gain + what moves + adoption order
- **W34** — 7ab3d17 (+parent): field fix 0c8fa21; settings page (marquee-config.kcfg/.qml, config_xml/config_qml/CONFIG_MODEL, cfg reads in main; kcfg default = solved alpha; parity 9/9; ghost-surfaces 30/30; witness pins); COTYPE s90/s90b
- **W35** — 
- **W36** — 
- **W37** — 
- **W38** — 54fdbe8: tickerText/pendingText + swapRing at the rotation's onFinished (loops: 1, restart), idle swaps immediately; lint clean; parity re-captured; witness pins; COTYPE s91. Live confirmation after re-emerge is the operator's.
- **W39** — a78d873: make_palette.rehue + hue_table (60/72 accepted, fallbacks are the gate); scripts/check_rehue.py @REHUE (worklist 61); parser collapses whitespace so run offsets are exact (harness 15 cases); parity 10/10; COTYPE s93

## residue

- (none)
